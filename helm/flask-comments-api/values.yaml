namespace: flask-demo

app:
  name: flask-comments-api

image:
  repository: 340528523115.dkr.ecr.eu-north-1.amazonaws.com/flask-comments-api
  tag: "2.0"
  pullPolicy: Always

replicaCount: 1

container:
  port: 5000

service:
  type: NodePort
  port: 5000
  targetPort: 5000
  nodePort: 30080

config:
  DB_PATH: /data/comments.db

persistence:
  enabled: true
  size: 1Gi
  mountPath: /data
