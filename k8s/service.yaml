apiVersion: v1
kind: Service
metadata:
  name: flask-comments-api
  namespace: flask-demo
  labels:
    app: flask-comments-api
spec:
  type: NodePort
  selector:
    app: flask-comments-api
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080
